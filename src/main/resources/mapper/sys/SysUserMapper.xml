<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cxs.sys.sysuser.dao.SysUserMapper">

    <select id="getUserModelWithBlurry" parameterType="java.lang.String"
            resultType="com.cxs.sys.sysuser.model.SysUserModel">
        SELECT
        *
        FROM
        SYS_USER
        WHERE
        LOGIN_NAME = #{loginName}
        OR PHONE = #{loginName}
        OR EMAIL = #{loginName}
    </select>

    <select id="getRole" parameterType="java.lang.String" resultType="java.lang.String">
        SELECT
        SR.CODE
        FROM
        SYS_ROLE SR
        LEFT JOIN
        SYS_USER SU
        ON
        SR.ID = SU.ROLE_ID
        WHERE
        SU.LOGIN_NAME = #{loginName}
        OR SU.PHONE = #{loginName}
        OR SU.EMAIL = #{loginName}
    </select>
</mapper>